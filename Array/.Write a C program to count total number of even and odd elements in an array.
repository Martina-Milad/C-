#include <stdio.h>
#define MAXSIZE 100

int main()
{
    int arr[MAXSIZE] ,i , size ,even =0,  odd =0 ;
	printf("enter size of array ");
	scanf("%d" , &size);
	printf("Input array elements: ");
	for(i = 0 ; i<size ;i++){
		scanf("%d", &arr[i]);
	}
	

	for(i=0 ;i<size ; i++)
	{
		if(arr[i]%2 ==0)
		{
			even = even +arr[i];
		}
		else if (arr[i]%2 ==1 )
			odd= odd+arr[i];
	}
		printf("total even numbers  = %d" , even);
		printf("total odd numbers  = %d" , max2);



}
